{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install && cp .env.example .env && php artisan key:generate && npm install && npm install sass --save-dev && npm run build"
  },
  "deploy": {
    "startCommand": "sleep 15 && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}